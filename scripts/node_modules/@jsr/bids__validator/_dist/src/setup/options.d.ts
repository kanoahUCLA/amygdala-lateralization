import type { LevelName } from "@jsr/std__log";
import { Command } from "@jsr/effigies__cliffy-command";
import type { Issue, Severity } from "../types/issues.js";
/**
 * BIDS Validator config file object definition
 */ export type Config = {
  [key in Severity]?: Partial<Issue>[];
};
/**
 * BIDS Validator options object definition
 */ export type ValidatorOptions = {
  datasetPath: string;
  schema?: string;
  config?: string;
  json?: boolean;
  format?: string;
  verbose?: boolean;
  ignoreNiftiHeaders?: boolean;
  ignoreWarnings?: boolean;
  filenameMode?: boolean;
  debug: LevelName;
  color?: boolean;
  recursive?: boolean;
  outfile?: string;
  datasetTypes: string[];
  blacklistModalities: string[];
  prune?: boolean;
  maxRows?: number;
  preferredRemote?: string;
};
/** Extendable Cliffy Command with built in BIDS validator options */ export declare const validateCommand: Command<void, void, any, string[], void>;
/**
 * Parse command line options and return a ValidatorOptions config
 * @param argumentOverride Override the arguments instead of using Deno.args
 */ export declare function parseOptions(argumentOverride?: string[]): Promise<ValidatorOptions>;
//# sourceMappingURL=options.d.ts.map