import { yellow } from "@jsr/std__fmt/colors";
export function createTestFunction(runTest) {
  return (nameOrOptions, fn)=>{
    if (typeof nameOrOptions === "string") {
      return runTest({
        name: nameOrOptions,
        fn: fn
      });
    }
    if (nameOrOptions.ignore) {
      console.warn(yellow("skip: %s"), nameOrOptions.name);
    }
    return runTest(nameOrOptions);
  };
}
//# sourceMappingURL=create_test_function.js.map