import type { KeyCode } from "@jsr/cliffy__keycode";
import { GenericPrompt, type GenericPromptKeys, type GenericPromptOptions, type GenericPromptSettings } from "./_generic_prompt.js";
/** Generic prompt options. */ export interface ToggleOptions extends GenericPromptOptions<boolean, string> {
  /** Keymap to assign key names to prompt actions. */ keys?: ToggleKeys;
  /** Change active label. Default is `"Yes"`. */ active?: string;
  /** Change inactive label. Default is `"No"`. */ inactive?: string;
}
/** Toggle prompt settings. */ interface ToggleSettings extends GenericPromptSettings<boolean, string> {
  keys: ToggleKeys;
  active: string;
  inactive: string;
}
/** Toggle prompt keymap. */ export interface ToggleKeys extends GenericPromptKeys {
  /** Activate keymap. Default is `["right", "y", "j", "s", "o"]`. */ active?: string[];
  /** Deactivate keymap. Default is `["left", "n"]`. */ inactive?: string[];
}
/**
 * Toggle prompt representation.
 *
 * ```ts
 * import { Toggle } from "./mod.ts";
 *
 * const password: boolean = await Toggle.prompt("Please confirm");
 * ```
 */ export declare class Toggle extends GenericPrompt<boolean, string> {
  protected readonly settings: ToggleSettings;
  protected status: string;
  /** Execute the prompt. */ public static prompt(options: string | ToggleOptions): Promise<boolean>;
  constructor(options: string | ToggleOptions);
  public override getDefaultSettings(options: ToggleOptions): ToggleSettings;
  protected override message(): string;
  /** Read user input from stdin, handle events and validate user input. */ protected override read(): Promise<boolean>;
  /**
   * Handle user input event.
   * @param event Key event.
   */ protected override handleEvent(event: KeyCode): Promise<void>;
  /** Set active. */ protected selectActive(): void;
  /** Set inactive. */ protected selectInactive(): void;
  /**
   * Validate input value.
   * @param value User input value.
   * @return True on success, false or error message on error.
   */ protected validate(value: string): boolean | string;
  /**
   * Map input value to output value.
   * @param value Input value.
   * @return Output value.
   */ protected transform(value: string): boolean | undefined;
  /**
   * Format output value.
   * @param value Output value.
   */ protected format(value: boolean): string;
  /** Get input value. */ protected getValue(): string;
}
//# sourceMappingURL=toggle.d.ts.map